# Provider Registry — Nexora

The provider registry is the authoritative inventory of provider adapters and normalized capabilities.

Each provider SHOULD include or derive the following compatibility metadata:

- stable `providerId`
- provider adapter version
- supported model families
- streaming support
- resume support
- minimum compatible API/SDK contract version
- usage-accounting support level
