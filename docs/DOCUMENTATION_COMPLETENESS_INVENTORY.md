# Nexora Documentation Completeness Inventory

> **Status: SUPPORTING** repository-wide documentation inventory. This document does not create or modify an architecture decision, canonical authority, implementation artifact, or lifecycle owner.
>
> **Purpose:** record the current documentation coverage and preserve unresolved downstream work without converting absence of implementation into absence of architecture.

## Audited domains

The repository corpus was inventoried across:

- Session, Conversation, checkpoint, Runtime, Task, Execution, Context, Memory, Agent, Workflow, Tool, Skill, Workspace, Provider, Plugin, persistence, API, security, recovery, state machines, protocols, testing, configuration, deployment, and Android/application boundaries.
- Requirements, assumptions, constraints, risks, architecture, decisions, models, lifecycle documents, specifications, APIs, registries, security documents, testing documents, roadmap, repository structure, and traceability sources.

## Coverage status

- Models: present for the documented domain entities under `models/`; no missing model is promoted to an architectural defect solely because an implementation type is absent.
- Architecture: present under `architecture/`; ownership remains governed by the cited decision or architecture document, not by naming alone.
- Lifecycles/state machines: present for documented lifecycle domains under `lifecycle/` and `state-machines/`; a lifecycle/state machine is not inferred for a domain where no repository source establishes one.
- Protocols/APIs: present for the documented Agent, Execution, Memory, Plugin, Provider, and Tool boundaries, with API documents under `docs/api/`; concrete transport and implementation details remain downstream where the source says so.
- Engineering specifications: present where the repository has selected a semantic contract, including persistence/schema, background execution, environment, workspace, terminal, checkpoints, and Session–Conversation handoff contracts.
- Runtime/recovery: documented for Session–Conversation recovery and existing runtime lifecycle material; process/platform-specific recovery mechanisms remain implementation choices unless selected by a canonical source.
- Errors/validation/security: documented through `errors/`, `security/`, relevant specifications, and domain contracts; numeric error encoding and concrete enforcement mechanisms remain downstream where not canonically selected.
- Testing: present through test strategy documents, case documents, and the Session–Conversation deterministic matrix; planned evidence directories are not treated as executed test evidence.
- Traceability/canonical sources: maintained through `docs/TRACEABILITY.md` and `docs/CANONICAL_SOURCES.md`.
- Creator-owned product design: present at `NEXORA_PRODUCT_DESIGN_BY_CREATER.md`; it defines what Nexora is, does not replace canonical subsystem ownership, and is protected by the conflict-stop-report-creator-decision protocol.

## Architecture-resolution status

### Active OPEN/DEFERRED register

At the current published HEAD, the active engineering contract set has no unresolved approval-expiry or cloud-only AI-provider scope boundary. DEC-36 classifies an approval transaction that expires before a valid authorization outcome as the existing `NXR-2003 / POLICY_DENIAL` authorization-gate denial, while preserving expiry as a distinct audit, trace, notification, and activity-feed outcome. Most product, architecture, ownership, lifecycle, protocol, security, and terminology decisions in this inventory are resolved or explicitly bounded downstream. The former `NFR-PERF-006` and `NFR-REL-004` requirement-authority collisions are resolved: the original performance and persistence meanings remain canonical, while the execution-mode and bounded-progress meanings now use `NFR-CI-006` and `NFR-CI-007`. The repository still contains historical unresolved wording, bounded downstream choices, and planned evidence obligations; these categories are not interchangeable with an active owner decision.

DEC-36 does not create a new NXR code, subreason, lifecycle state, transition, retry path, or authorization bypass. `USER_DENIED` remains explicit user rejection of an ASK approval, and `MALFORMED_APPROVAL` remains invalid transaction structure or correspondence.

The 250-item research-workload target remains a **GENUINE OWNER DECISION**. The repository identifies candidate meanings—accepted workload size, queued items, completed items, planned subtasks, concurrent workers, or a benchmark—and preserves existing resource, dependency, and liveness constraints. No normative limit is established.

The private-reasoning boundary is **RESOLVED-BY-DESIGN** at the documentation level. `specs/CONTEXT_MANAGEMENT.md` §6.2 is the semantic authority: durable reasoning artifacts contain redacted approach/evidence/decision/uncertainty/verification information and exclude unrestricted private chain-of-thought, hidden prompts, credentials, and raw untrusted content. `architecture/AGENT_RUNTIME.md`, `models/Inference.md`, `architecture/RUNTIME.md`, `specs/DATABASE_SCHEMA.md`, `protocols/Provider-Protocol.md`, `protocols/Agent-Protocol.md`, `docs/api/Agent-API.md`, and `ui/Components.md` project that same boundary through execution traces, persistence, provider events, API events, and user-visible cards. `docs/api/Runtime-API.md` now explicitly treats checkpoint history as structured recovery/audit data rather than raw private reasoning. No new reasoning object, schema, requirement, lifecycle state, or error identity is introduced.

Adaptive deep-agent orchestration is **CONTRACT-COMPLETE / IMPLEMENTATION AND EVIDENCE GAP** rather than a missing architecture. Existing owners cover the chain from deliberation, planning, context and evidence assembly, through dependency analysis, worker allocation, sequential/parallel execution, observation, evidence aggregation, conflict handling, bounded repair/re-planning, verification, checkpoint/recovery, and completion gating: `architecture/AGENT_RUNTIME.md` owns the single-agent loop, `architecture/MULTI_AGENT_SYSTEM.md` owns delegation and parallel coordination, `architecture/WORKFLOW_ENGINE.md` owns graph progression and bounded cycles, `specs/CONTEXT_MANAGEMENT.md` owns reconstructable long-horizon projections and claim evidence, and Tool/Provider/lifecycle authorities retain their respective boundaries. `docs/api/Runtime-API.md` and `docs/TRACEABILITY.md` now project these contracts without creating a second owner or state machine. Planned validation remains non-executed evidence, including `E2E-ORCH-001`, `E2E-MA-001`, `E2E-REL-007..009`, `E2E-LONG-001..003`, `IT-CONTRACT-002`, `IT-LIVE-001..006`, `UT-AS-010`, and `UT-EV-007`.

The historical `OWNER DECISION REQUIRED / UNRESOLVED` passages in DEC-13 and DEC-14 are preserved decision records. Their formerly open boundaries are superseded or narrowed by DEC-15 through DEC-24 and DEC-25 through DEC-40: Session–Conversation ownership, relationship identity/representation/cardinality/lifecycle/continuation, BranchLineage ownership and operational policy, checkpoint metadata semantics and retention, no internal credit/cost gating, Android-first positioning, requirement-owner taxonomy, ROADMAP terminology, execution-failure binding, agent-loop liveness, Task liveness error identities, background terminal parent binding, approval-denial composition, approval-expiry classification, and workflow scope are now represented by later canonical decisions. DEC files remain immutable historical authority and are not rewritten.

The remaining bounded downstream choices are not OPEN/DEFERRED architecture decisions: concrete identifier encoding, message/turn ordering representation, storage/schema/serialization, API/transport names and DTO shapes, transaction/concurrency mechanisms, Android component realization, exact DAO/migration mechanics, terminal-session persistence encoding, and implementation-level cleanup scheduling details where the governing decision already fixes the required semantic outcome. DEC-34 fixes autonomous background terminal parent binding, deadline inheritance, and reconciliation; concrete process cleanup and persistence mechanics remain downstream. Planned unit, integration, performance, security, regression, and Android/device evidence is also not a missing architecture decision; it is an implementation/evidence obligation and must not be reported as executed evidence.

Other unresolved values allowed by this inventory are local `UNKNOWN` or `UNAVAILABLE` data states explicitly defined by a contract, such as Tool `UNKNOWN_COMPLETION` pending reconciliation or provider health before its first check. They are runtime states or evidence classifications, not owner decisions.

The current inventory does not treat the following as active OPEN/DEFERRED architecture gaps:

- Tool timeout (`NXR-2002`) is an execution outcome with no ToolStatus lifecycle effect; unknown completion and conditional retry remain governed by `architecture/TOOL_SYSTEM.md` and the canonical error mapping.
- `listAgents` has no collection-specific canonical error mapping; an empty collection result is not `NXR-3001`.
- Blocked-app interaction preserves the established classification-denial compatibility outcome (`NXR-2003` / `CLASSIFIER_DENIAL`) when an applicable classification boundary denies access; no local AI classifier is active. Denial, audit, notification, isolation, no bypass, no automatic resume, and new-operation/task continuation remain authoritative.
- Provider `Retry-After` and Task `RetryPending` retain independent scopes and do not create a merged retry state.
- Session–Conversation semantic questions selected by DEC-13 through DEC-21 are closed; historical unresolved wording does not reopen relationship status, ownership, identity, representation, cardinality, lifecycle, or continuation semantics. DEC-22 and DEC-23 separately resolve BranchLineage ownership and checkpoint retention/deletion safety.
- Concrete Conversation/checkpoint mechanisms—metadata representation/encoding, message/turn ordering representation, storage technology, schema implementation, API/transport, and recovery mechanism—remain bounded implementation choices within the selected semantic invariants; no concrete mechanism is selected by this inventory.
- BranchLineage artifact ownership is resolved by DEC-22, and its operational identity/lifecycle/retention/deletion/quota/cleanup policy is resolved by DEC-31: a distinct BranchLineage artifact owns rollback parent/source lineage, uses `RECORDED`/`ACTIVE`/`DETACHED`/`DELETED`, protects dependent checkpoints, and applies the selected 30-day superseded retention and 100-checkpoint workspace quota. Concrete storage encoding and API transport remain implementation projections within those invariants.
- Requirement identifiers `FR-SESS-001`, `FR-WF-001`, and `NFR-COMP-001` are now defined and mapped. Their implementation and executed validation remain planned or partial because the repository has no source implementation or executed evidence.
- Requirement identity separation is resolved: `NFR-PERF-006` remains active-agent battery impact, `NFR-REL-004` remains ACID data persistence, `NFR-CI-006` is minimum-sufficient execution-mode selection, and `NFR-CI-007` is bounded-progress control. Their dependent mappings are canonical and no requirement meaning was deleted or merged.
- Execution-failure responsibility and class binding are resolved by DEC-29: `errors/ERROR_CODES.md` owns error identity and shared recovery metadata, the owning lifecycle owns legal lifecycle effect, and the operation owner owns idempotency and retry conditions. Transient, permanent, and escalation classifications do not create new lifecycle states; legal Task/Execution effects remain those of the existing canonical state machines. DEC-33 adds the exact Task liveness error identities `NXR-1014`, `NXR-1015`, and `NXR-1016`.
- Conversation-local metadata semantic boundary is resolved by DEC-24: creation provenance and integrity information required to interpret and validate the Conversation record are included; concrete field names, types, encoding, schema, storage, API, and implementation remain downstream.
- TM-008 and TM-037 are planned security-completion work with established owners, Phase 7 targets, acceptance criteria, and residual risks. TM-002, TM-003, TM-004, TM-007, TM-009, TM-010, TM-012, TM-013, TM-014, TM-015, TM-023, TM-024, and TM-025 remain OPEN/DEFERRED bookkeeping gaps because the Threat Model’s required owner, target phase, acceptance criterion, and residual-risk fields are blank; no values are inferred. The primary STRIDE catalog now uses the same OPEN/DEFERRED classification for these rows, eliminating the former Mitigated-versus-deferred status contradiction without changing their documented controls.
- Workflow Engine and WorkflowLifecycle now explicitly delegate approval transactions, denial/expiry effects, retry/error identity, Tool/Provider retry legality, deadline/cancellation inheritance, checkpoint persistence, UNKNOWN_COMPLETION reconciliation, and bounded-cycle validation to existing canonical authorities; no new Workflow state or error is introduced. Workflow-specific validation remains Planned through the existing `IT-CONTRACT-002` projection.
- Android trigger and recovery semantics are now explicitly bounded in `specs/BACKGROUND_EXECUTION.md`: process death, app termination, reboot, OEM termination, Doze/App Standby deferral, battery degradation, and force-stop are runtime triggers or conditions, not Task or Execution states. They do not independently imply success, cancellation, failure, deadline renewal, or replay permission; later recovery must inspect durable state and apply existing lifecycle, checkpoint, idempotency, and reconciliation rules. Validation remains planned through `NFR-REL-009`, `E2E-REL-001..004`, `E2E-ESC-004`, and `IT-LC-020`.
- Embedded runtimes and `TOOL-403`/`TOOL-404` streaming remain planned/later work; reserved tool IDs are not architecture gaps or reusable identifiers.
- JavaScript-scripted workflows and a dedicated `/workflows` monitoring panel are explicitly out of the current release scope under DEC-32; the declarative Workflow Engine and existing activity-feed projections remain authoritative, and no JavaScript runtime or dedicated panel implementation is claimed.
- The Product Vision exclusivity wording is resolved by DEC-26: Android-first positioning is preserved without presenting market exclusivity as a verified fact.

The Product Vision positioning, requirement owner labels, ROADMAP terminology boundaries, execution-failure class binding, agent-loop liveness boundaries, Task liveness error identities, background terminal liveness, approval-denial composition, approval-expiry classification, Android build target, Full Environment packaging gate, GGUF process boundary, module interface/EventBus boundary, cloud-only AI-provider scope, no-local-AI-model security boundary, and non-functional requirement identity separation are resolved by DEC-26 through DEC-43. DEC-31 selects 30-day superseded checkpoint retention, a 100-checkpoint per-workspace quota, and daily idempotent cleanup; DEC-34 selects parent binding, deadline inheritance, and orphan reconciliation for autonomous background terminals; concrete storage encoding, transaction mechanism, process cleanup, and transport remain downstream implementation choices. Concrete Conversation metadata fields and schema remain downstream under DEC-24.

Agent reliability hardening is now documented without claiming implementation completion. `NFR-REL-016` covers hierarchical deadline propagation, `NFR-REL-017` covers unknown-completion reconciliation, `NFR-REL-018` covers repeatable Android reliability evidence, `NFR-CI-003` covers claim-to-evidence binding, `NFR-CI-004` covers acceptance-criteria progress, `NFR-CI-005` covers non-overridable reasoning ceilings, `NFR-CI-006` covers minimum-sufficient execution-mode selection, and `NFR-CI-007` covers bounded-progress controls. The ClaimRecord projection, final post-repair claim validation, learning-lesson boundary, and trust-state boundary are now represented in the derived models and contract projections. `testing/cases/E2ETestCases.md` defines the planned `E2E-REL-001` through `E2E-REL-009` evidence cases, while `testing/cases/UnitTestCases.md` defines `UT-AS-004`, `UT-AS-005`, `UT-AS-010`, `UT-CONTRACT-006`, `UT-EV-007`, `UT-RN-013`, and `UT-TOOL-006`. These remain planned validation obligations, not proof that implementation or device tests already exist. `UT-EXEC-001` covers DEC-29 failure-class binding and remains planned.

The documentation has been elevated to an enterprise-grade autonomous agent framework standard, incorporating **Background Execution Resilience** (`specs/BACKGROUND_EXECUTION.md`), **Atomic Side-Effect Capability** (`architecture/TOOL_SYSTEM.md`), **Competitive Multi-Agent Consensus Verification** (`architecture/MULTI_AGENT_SYSTEM.md`), **Cross-Workspace Skill Distillation** (`specs/AUTONOMY_STABILITY.md`), and **Recursive Task Graph with Dynamic Plan Refinement** (`specs/EXECUTION_LIFECYCLE.md`).
 This pass strengthens the existing contracts for `ProgressSignal`, plan-step acceptance, ReasoningPolicy mapping, LearningLesson validation, and passive untrusted-content handling. Proven derived-document drift was corrected for Tool catalog count, Permission scope enumeration, Workspace lifecycle states, Context section numbering, workspace-switch target wording, and the `maxExecutionDepth` registry field. Sandbox, provider, context, checkpoint, approval, UI, and observability semantics remain governed by their existing canonical documents; no second owner, new lifecycle state, new error, new requirement, or new persistence table is introduced. Planned validation remains non-executed evidence.

## Unresolved classification

The following are not silently promoted to architecture decisions:

- implementation module existence and package layout: **PLANNED/DOWNSTREAM** where identified by roadmap or project structure;
- concrete database tables, Room entities, DAOs, serialization, migrations, and storage engines: **IMPLEMENTATION CHOICE/DOWNSTREAM** unless a canonical specification selects them;
- concrete API endpoint names, DTOs, transport, event schemas, and idempotency mechanisms: **IMPLEMENTATION CHOICE/DOWNSTREAM** unless selected by a canonical API source;
- Android process restoration, scheduling, background execution mechanism, and deployment packaging: **DOWNSTREAM/IMPLEMENTATION CHOICE** subject to the existing Android/application boundary documentation;
- retention durations, cleanup timing, quotas, operational metrics, and migration execution procedures: **UNRESOLVED/DOWNSTREAM** where no canonical source selects values or mechanisms; DEC-31 is authoritative for checkpoint retention, quota, and cleanup values, while concrete execution mechanisms remain downstream;
- ownership questions not explicitly established by a decision or canonical architecture document: **OWNER DECISION REQUIRED**, with no owner inferred from terminology.
- Task dependency liveness is resolved by DEC-30: Task dependency graphs are validated acyclic before queueing; failed or cancelled dependencies make dependents fail; Pending/Blocked work inherits the effective deadline and fails on expiry; Workflow validation is not reused as Task authority.
- Task approval denial and external-wait state effects are resolved by DEC-30/DEC-35/DEC-36: explicit denial fails the Task through existing `NXR-2003`/`USER_DENIED`; an expired approval fails the Task through existing `NXR-2003`/`POLICY_DENIAL` under DEC-36 without automatic approval or retry; expiry remains distinct in audit and activity projections; unanswered `BlockedAwaitingInput` fails on the effective deadline; no new state is created.
- Agent failure retry identity is resolved by DEC-30: committed `AgentStatus.FAILED` is terminal for the runtime incarnation; retry creates a new incarnation/version and execution identity while preserving the stable registered `agentId` and predecessor linkage.
- Provider rate-limit wait bounds are resolved by DEC-30: valid Retry-After is bounded by the parent effective deadline; missing/invalid/repeated delays use bounded backoff; deadline exhaustion removes the queue item, checkpoints state, and applies the existing task-deadline failure effect.
- Delegation depth is resolved by DEC-30 at maximum active depth 4; requests beyond the bound fail with existing `NXR-3011` coordination semantics and return to parent replanning. Parent deadlines, concurrency, duplicate-scope, and deadlock controls remain independent.
- DEC-34 resolves autonomous background terminal liveness: every agent-created background session is bound to its parent Task/Execution and immutable effective deadline; parent cancellation, terminal state, or deadline expiry invokes existing termination/checkpoint recovery; missing or terminal parents are reconciled without new TerminalSession states.
- DEC-35 resolves approval-denial composition: `NXR-2003` remains the Tool/Permission result with no side effect; the owning Task commits `Failed`; the participating Agent may project `Paused`; later work requires a new authorization transaction and is not an automatic retry.

## Pre-source implementation-readiness status

The pre-source audit identified and resolved the documentation contracts required before Android source bootstrap. The requirement authority now contains 327 rows representing 327 unique FR/NFR identifiers (245 FR + 82 NFR), with the former duplicate meanings separated into `NFR-CI-006` and `NFR-CI-007`. DEC-37 fixes the current API 34 min/compile/target baseline. DEC-38 separates minimal-foundation APK gates from the 80 MB architecture-specific Full Environment delivery gate. DEC-39 is retained as historical local-GGUF boundary authority but its active scope is superseded by DEC-41. DEC-40 permits public-interface dependencies along the canonical module graph and reserves EventBus for event transport. DEC-41 fixes cloud/external-only AI providers and cloud-only degradation (superseded in active scope by DEC-44). DEC-42 retires the optional local TFLite classifier and fixes the no-local-AI-model boundary (superseded in active scope by DEC-44). DEC-44 elevates the combined DEC-41/DEC-42 boundary into a permanent, non-revisit repository rule (Rule 9 of `AGENTS.md`).

The canonical persistence schema now explicitly maps the lifecycle-critical Agent and Task versions, required Task recovery/output fields, Execution `latestErrorJson`, ToolInvocation `completionState` and reconciliation evidence, and replay certainty. The Execution Protocol now owns `TaskSuspendedEvent` and `CheckpointSavedEvent`; these are events and do not create Task or Execution lifecycle states. Coding standards and traceability are synchronized to the canonical module, schema, event, target, packaging, and provider decisions.

These resolutions close documentation contradictions and source-generation ambiguity. They do not claim Android source, build, runtime, packaging, device, or test implementation. The active implementation environment must still verify JDK 21, Gradle 8.10.2, Kotlin, Android SDK/build tools API 34, ADB, and required environment variables before source bootstrap. Planned validation remains evidence work rather than a documentation gap.

## External capability comparison implementation handoff

The external capability comparison is supporting evidence only. It does not override Nexora’s canonical decisions, owners, lifecycles, security rules, error catalog, provider scope, or pure-Android classification. The following twenty domains are the implementation handoff status for the capabilities identified by that comparison:

1. **Android application and UI:** UI, Task, Execution, permission, notification, background, accessibility, partial-result, retry, and reconciliation contracts are documented. Android source and device evidence remain future work.
2. **Agent planning and deep work:** the bounded FAST/NORMAL/DEEP/VERIFY/RECOVER loop, ReasoningPolicy, evidence gates, progress vectors, repair, escalation, and completion gates are contract-complete. Planner quality, model routing quality, and long-run evidence remain future work.
3. **Agent-loop liveness:** progress, dependency, deadline, retry, cancellation, provider-wait, approval-wait, anti-loop, checkpoint, recovery, and terminal contracts are documented. Stress execution is not yet evidence.
4. **Long-running and background execution:** Foreground Service, WorkManager, checkpoint, notification, deadline, Android degradation, and restart contracts are documented. Trigger reconciliation, process death, OEM, Doze, battery, and force-stop validation remain future work.
5. **Process death and resume:** durable identity, checkpoint, parent binding, deadline, cancellation, and reconciliation semantics are documented. Device, OEM, ANR, reboot, and restart evidence remain future work.
6. **Persistent task, session, workspace, and checkpoint state:** Room shape, delegated stores, identity/version fields, lineage, retention, quota, cleanup, and replay certainty are documented. Room implementation, migrations, crash-atomic commits, and restore evidence remain future work.
7. **Parallel agents and orchestration:** delegation, fresh/private overlays, bounded fan-out, dependency ordering, parent/child lineage, artifacts, conflict abstention, deadlock handling, cancellation, and terminal roll-up are documented. Worker scheduling and scale evidence remain future work. The 250 value is not a concurrent-agent limit.
8. **Tools, browser, computer interaction, and side effects:** Tool, Browser, Terminal, Git, permission-before-side-effect, idempotency, UNKNOWN_COMPLETION, reconciliation, cancellation, and audit contracts are documented. Adapters and side-effect integration evidence remain future work.
9. **Filesystem, terminal, sandbox, and isolation:** workspace roots, process limits, memory and disk quotas, egress proxy, workspace isolation, plugin containment, cleanup, and violation responses are documented. Executable enforcement and penetration evidence remain future work.
10. **Permissions, approvals, security, and user control:** scope hierarchy, ASK/DENY/ALLOW, approval transactions, expiry, revocation, escalation, fail-closed behavior, audit, no-side-effect denial, and no automatic resume are documented. Approval UI and security evidence remain future work.
11. **Web research and information gathering:** Browser, retrieval, ContextSnapshot, evidence classification, claim binding, freshness, contradiction, confidence, and output blocking are documented. Search/browser connectors, extraction, citation capture, and research execution evidence remain future work.
12. **Code generation and repository workflows:** terminal, filesystem, Git, workspace, sandbox, permission, checkpoint, and delegated-artifact boundaries are documented. Build/test adapters and repository-side-effect evidence remain future work.
13. **Skills and capability representation:** Skill Registry, Plugin lifecycle, compatibility, permission inheritance, sandbox, versioning, revocation, replacement, and audit boundaries are documented. Discovery, loading, progressive disclosure, safe script review, composition, and UX evidence remain future work. Community distribution is not implied.
14. **Context, memory, long context, and cross-step state:** ContextSnapshot, retrieval diversity, compaction lineage, provenance, claim records, stale/conflicting evidence handling, memory lifecycle, conversation identity, and offline non-inference persistence are documented. Retrieval quality, reconstruction, and cross-task isolation evidence remain future work.
15. **Provider/model routing and streaming:** cloud-only provider scope, capability negotiation, model snapshots, typed stream envelopes, ordering, backpressure, Retry-After, cancellation, failover lineage, error mapping, and private-artifact boundaries are documented. Provider adapters and stream/failover evidence remain future work.
16. **Observability, progress, logs, artifacts, and user-visible status:** correlation, progress vectors, audit, artifacts, lifecycle events, partial output, unknown completion, approval, retry, notifications, and UI projections are documented. Event publication, storage, notification delivery, artifact indexing, and any future webhook surface require implementation and a separately scoped API decision if needed.
17. **Human-in-the-loop intervention:** approval, denial, escalation, user input, cancellation, browser boundaries, terminal boundaries, notification routing, and visible status are documented. Concrete Android takeover/session attachment requires a targeted protocol/API/UI projection if the product scope includes it.
18. **Multi-step workflows and scheduled tasks:** declarative Workflow Engine, bounded iterations, approval, retry/error ownership, checkpoint, cancellation, Android scheduling, and current out-of-scope JavaScript workflow/panel decisions are documented. User-facing recurring schedule semantics—recurrence, timezone, missed runs, pause/edit/delete, notification, retry, and history—remain a product decision if expanded beyond the current scope.
19. **Resources, scale, deadlines, and reliability:** technical token, call, time, CPU, memory, battery, storage, stream, concurrency, deadline, progress, failure-ledger, and recovery contracts are documented. The 250-item research workload remains a **GENUINE OWNER DECISION** with no normative limit established (see the active OPEN/DEFERRED register above); candidate meanings are preserved without selecting one. Measurements and stress evidence remain planned future work. The external comparison’s vendor claim remains supporting evidence only and does not override the Nexora definition.
20. **Isolation across tasks, users, workspaces, tools, and external environments:** Workspace, Task, Execution, Conversation, Memory, Provider, Pipe, Tool, Plugin, sandbox, overlay, artifact, and audit boundaries are documented. Cross-user sharing, shared projects, and artifact-sharing semantics are not inferred and require a separate product/security decision if desired.

This comparison-derived handoff therefore identifies predominantly implementation/evidence work rather than missing architecture. New architecture is not justified merely because an external product demonstrates a capability. Any future capability that changes product scope, cross-user access, authenticated browser takeover, arbitrary Android application/system-UI control, recurring schedule semantics, or parallel scale must receive its own explicit requirement, owner, security, lifecycle, persistence, API/protocol, traceability, and validation treatment.

## Contradiction and stale-claim handling

Historical decision text is preserved as historical record. Active engineering documents must not treat historical unresolved wording as current authority after a later decision closes the matter. Negative statements about prohibited relationship identity, representation, lifecycle, and cardinality are not stale claims; they are explicit non-decisions or forbidden inferences under DEC-17 through DEC-20.

## Validation record

- Relative Markdown links: validated against the repository filesystem.
- Canonical-source and traceability references: validated for referenced Markdown paths.
- Implementation scope: documentation inventory only; no source implementation is created by this document.
- This inventory is supporting evidence and does not replace any decision, canonical source, model, protocol, specification, or test artifact.
- `standards/Documentation-Standard.md` §Implementation-Ready Contract Rule now makes the documentation corpus the pre-source design boundary: significant behavior must be specified before implementation; canonical ownership, lifecycle, protocol, API, security, persistence, recovery, privacy, resource, audit, and observable-outcome projections must remain explicit; unsupported behavior must remain `UNKNOWN`, `OPEN/DEFERRED`, `PROPOSAL`, or `OWNER DECISION REQUIRED`; and planned validation must not be presented as executed evidence.

## Implementation-handoff closure

For every documented domain, the existing model/architecture/lifecycle/state-machine/protocol/specification/API/security/testing artifacts are the authoritative evidence set when present. The following cross-domain rules are explicit for implementation handoff:

- Runtime may coordinate documented protocols but does not acquire ownership that a canonical source assigns to another domain.
- Session, Task, Execution, Context, Memory, Workspace, Provider, Plugin, Tool, and Workflow identities remain distinct unless a canonical source explicitly defines a relationship.
- Process death, restart, crash, retry, cancellation, timeout, shutdown, persistence failure, provider failure, plugin failure, authorization failure, quota exhaustion, and migration/version mismatch must preserve the owning domain's documented lifecycle and persistence invariants; no automatic semantic transition is inferred from the platform event alone.
- Duplicate requests and retries must follow the idempotency or repeat-submission rule of the owning protocol/specification; where no such rule exists, the operation is not granted implicit idempotency.
- Concrete schema, transport, Android component, package, and deployment mechanisms may be selected downstream only if they preserve the documented identity, ordering, lifecycle, authorization, concurrency, recovery, cleanup, and compatibility invariants.

## Adversarial scenario coverage

The repository's domain specifications and test strategy documents are the governing sources for adversarial implementation validation. The Session–Conversation matrix explicitly covers process death, restart, concurrency conflicts, rollback, continuation, recreation, checkpoint recovery, and identity/lineage violations. Other domains retain their scenario coverage in the corresponding lifecycle, protocol, security, API, and testing documents; absence of an executable test artifact is not treated as executed evidence.

## Requirement closure

Checkpoint requirements FR-CB001 through FR-CB006 no longer contain a stale decision-dependent TBD marker. Their semantic coverage is derived from the existing checkpoint decisions and supporting engineering contracts; concrete persistence and transport mechanisms remain bounded downstream choices.

## Domain evidence index

The following index records the repository evidence used for each current domain classification. “Contract complete” means semantic obligations and boundaries are documented even when concrete implementation mechanism remains selectable.

- Session — canonical: `models/Session.md`, `state-machines/SessionLifecycle.md`; supporting: `lifecycle/SessionLifecycle.md`, `specs/SESSION_CONVERSATION_ENGINEERING_CONTRACT.md`, `specs/SESSION_CONVERSATION_RUNTIME_RECOVERY.md`, `specs/SESSION_CONVERSATION_ERRORS.md`; tests: `testing/SESSION_CONVERSATION_TEST_MATRIX.md`; boundary: Session identity/lifecycle is distinct from Conversation identity and terminal reuse is forbidden.
- Conversation — canonical: `models/Conversation.md`, `decisions/DEC-13-conversation-identity-persistence.md`; supporting: `architecture/CONVERSATION_CHECKPOINTS.md`, `specs/CONVERSATION_CHECKPOINTS.md`, `specs/DATABASE_SCHEMA.md`, Session–Conversation contracts; tests: Session–Conversation matrix; boundary: immutable identity, ordered records, checkpoint/lineage preservation, and non-destructive branching.
- Checkpoint — canonical: `architecture/CONVERSATION_CHECKPOINTS.md`, `decisions/DEC-8-conversation-checkpoint-rollback.md` through `DEC-10-conversation-checkpoint-ownership.md`; supporting: `specs/CONVERSATION_CHECKPOINTS.md`, `specs/SESSION_CONVERSATION_ERRORS.md`; tests: T10–T13, T25; boundary: checkpoint identity and lifecycle remain distinct from Conversation and relationship identity, while BranchLineage owns rollback parent/source lineage under DEC-22.
- Agent loop and liveness — canonical: `architecture/AGENT_RUNTIME.md`, `state-machines/AgentLifecycle.md`, `state-machines/TaskLifecycle.md`, `specs/AUTONOMY_STABILITY.md`, `docs/api/Agent-API.md`, `docs/api/Runtime-API.md`; supporting: `models/Agent.md`, `models/Execution.md`, `protocols/Agent-Protocol.md`; boundary: objective, mode selection, progress, anti-loop controls, deadlines, repair/replanning, verification, checkpoint, cancellation, escalation, and terminal outcomes remain Agent Runtime and lifecycle responsibilities.
- Multi-agent orchestration — canonical: `architecture/MULTI_AGENT_SYSTEM.md`; supporting: `models/Agent.md`, `models/Task.md`, `models/Execution.md`, `protocols/Agent-Protocol.md`, `registry/AGENT_MATRIX.md`, `docs/api/Agent-API.md`; boundary: delegation depth, parent/child identity, capability inheritance, private overlays, file locks, worker allocation, partial results, artifact promotion, conflict handling, deadlock detection, cancellation, and terminal roll-up remain coordinator/delegation responsibilities and do not redefine the single-agent loop.
- Workflow and task progression — canonical: `architecture/WORKFLOW_ENGINE.md`, `state-machines/WorkflowLifecycle.md`, `state-machines/TaskLifecycle.md`; supporting: `models/Workflow.md`, `models/Task.md`, `docs/api/Runtime-API.md`; boundary: graph progression, dependency validation, approval/deadline/retry effects, cycle handling, and workflow completion remain workflow/task authorities.
- Context, memory, inference, and provider streaming — canonical: `specs/CONTEXT_MANAGEMENT.md`, `architecture/MEMORY_SYSTEM.md`, `architecture/PROVIDER_SYSTEM.md`, `state-machines/ProviderStreamLifecycle.md`; supporting: `models/Inference.md`, `models/Memory.md`, `models/Provider.md`, `protocols/Provider-Protocol.md`, `docs/api/Provider-API.md`, `sdk/ProviderSDK.md`; boundary: ContextSnapshot, evidence, ReasoningPolicy, redacted ReasoningSummary, cloud-only routing, provider capability negotiation, stream identity/ordering, failover lineage, backpressure, cancellation, and privacy remain subsystem contracts.
- Android runtime and background execution — canonical: `specs/BACKGROUND_EXECUTION.md`, `architecture/RUNTIME.md`, `state-machines/TerminalSessionLifecycle.md`; supporting: `models/Execution.md`, `models/TerminalSession.md`, `protocols/Execution-Protocol.md`, `docs/api/Runtime-API.md`; boundary: Foreground Service/WorkManager selection, process death, reboot, OEM/Doze degradation, force-stop, deadline inheritance, checkpoint/reconciliation, notification, and parent-bound TerminalSession behavior remain Android/runtime projections and do not create lifecycle states from platform triggers.
- Tool and external-side-effect execution — canonical: `architecture/TOOL_SYSTEM.md`, `protocols/Tool-Protocol.md`, `docs/api/Tool-API.md`, `models/ToolInvocation.md`; supporting: `specs/BROWSER.md`, `specs/GIT.md`, `specs/TERMINAL.md`, `security/PermissionModel.md`, `security/SandboxPolicy.md`; boundary: authorization, idempotency, UNKNOWN_COMPLETION, reconciliation, non-replay, cancellation, audit, sandbox, Browser, Git, Terminal, Plugin, and external side effects remain operation-level contracts.
- Privacy, provenance, audit, and UI projections — canonical: `specs/CONTEXT_MANAGEMENT.md`, `specs/DATABASE_SCHEMA.md`, `standards/Logging-Standard.md`, `ui/Components.md`; supporting: `architecture/AGENT_RUNTIME.md`, `architecture/RUNTIME.md`, `models/Inference.md`, `protocols/Agent-Protocol.md`, `docs/api/Agent-API.md`, `docs/api/Runtime-API.md`; boundary: raw private reasoning, hidden prompts, raw untrusted content, credentials, and opaque provider-private continuation state remain excluded from persistence, logs, exports, APIs, and UI; redacted summaries and structured evidence/provenance are the permitted projections.
- Skill, Workspace, Plugin, and registry contracts — canonical: `architecture/PLUGIN_SYSTEM.md`, `state-machines/PluginLifecycle.md`, `registry/AGENTS.md`, `registry/PLUGINS.md`, `registry/SKILLS.md`, `registry/TOOLS.md`, `models/Workspace.md`; supporting: `sdk/PluginSDK.md`, `models/Plugin.md`, `security/PermissionModel.md`, `security/SandboxPolicy.md`; boundary: registry identity, lifecycle, capability, permission, workspace isolation, plugin trust, revocation, and artifact boundaries remain distinct and versioned.
- Persistence — canonical: `specs/DATABASE.md`, `specs/DATABASE_SCHEMA.md`, `specs/FILE_SYSTEM.md`, and domain-specific persistence sections; boundary: semantic identity, ordering, consistency, recovery, retention/deletion obligations, and migration compatibility are fixed; engine/schema encoding is selectable only within those invariants.
- API — canonical: `docs/api/*.md`, matching `protocols/`, `specs/`, and domain engineering contracts; boundary: semantic operations, preconditions, postconditions, authorization, errors, concurrency, retry/idempotency requirements, and compatibility obligations are fixed; endpoint/DTO/transport names are selectable.
- Security — canonical: `architecture/SECURITY_MODEL.md`, `security/PermissionModel.md`, `security/SandboxPolicy.md`, `security/ThreatModel.md`; supporting: `architecture/SANDBOX.md`, `errors/ERROR_CODES.md`; tests: security test specifications; boundary: deny/authorization/sandbox invariants cannot be weakened by implementation choice.
- Recovery — canonical: lifecycle/state-machine documents and `specs/SESSION_CONVERSATION_RUNTIME_RECOVERY.md`; boundary: process death, restart, crash, retry, restoration, cancellation, timeout, and shutdown cannot silently create semantic transitions not authorized by the owning domain.
- State machines/Lifecycle — canonical: matching files under `state-machines/` and `lifecycle/`; boundary: states, guards, legal/illegal transitions, terminal behavior, persistence and cleanup expectations are owned by the corresponding domain.
- Protocols — canonical: matching files under `protocols/`; boundary: participants, sequencing, validation, failure, retry, cancellation, timeout, ordering, concurrency, and ownership are preserved; wire representation is selectable where not fixed.
- Testing — canonical: `testing/*.md`, `testing/cases/*`, `testing/EVIDENCE_CONVENTIONS.md`, and the Session–Conversation matrix; boundary: every normative contract requires positive, negative, boundary, lifecycle, failure, recovery, security, persistence, API, and cross-domain validation as applicable.
- Configuration/Deployment/Android/application boundaries — canonical: `PROJECT_SPECIFICATION.md`, `README.md`, `docs/ENVIRONMENT_SETUP.md`, `specs/FULL_ENVIRONMENT.md`, `specs/BACKGROUND_EXECUTION.md`, `specs/GIT.md`, `docs/ROADMAP.md`, and Android/security architecture sources; boundary: planned modules, platform mechanisms, packaging, scheduling, and deployment choices are selectable only if documented lifecycle, persistence, security, resource, recovery, and compatibility invariants remain intact.
- Requirements/Architecture/Decisions/Registries/Roadmap/Repository structure — canonical: matching corpus plus `docs/TRACEABILITY.md` and `docs/CANONICAL_SOURCES.md`; boundary: requirements map to authority, projections, and tests, while planned directories are not represented as implemented.

## Resource and compatibility boundary

Retention, deletion, archive, cleanup, quota, resource exhaustion, operational metrics, migration, and version compatibility are classified per domain source. Where a numeric value or mechanism is absent, the implementation must preserve semantic safety: no unauthorized deletion, no identity/order/lineage loss, no silent incompatible migration, no bypass of quota/error semantics, and no loss of required audit/security evidence. Concrete durations, algorithms, metric names, migration tooling, and packaging remain selectable only when they preserve those constraints and are recorded in the relevant downstream design artifact.

## Legacy decision wording

Occurrences of “unresolved”, “future decision”, and “owner decision required” inside `decisions/` and historical decision projections are retained as historical decision evidence. They are not active gaps when a later decision and supporting contract supersede the earlier open question. The active inventory and engineering documents classify current implementation boundaries explicitly; no historical decision text is treated as a current conflicting authority.
