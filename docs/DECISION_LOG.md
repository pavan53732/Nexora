# Decision Log

> Back to [PROJECT_SPECIFICATION.md](../PROJECT_SPECIFICATION.md)

Smaller engineering decisions that don't warrant a full ADR.

| Date | ID | Decision | Context | Options Considered | Chosen | Rationale | Impact |
|------|-----|----------|---------|--------------------|--------|-----------|--------|
| 2025-01-15 | DL-001 | Kotlin-only for application code | Java still permitted in build logic and third-party libs | Kotlin-only, Kotlin + Java hybrid | Kotlin-only | Reduces cognitive overhead; Kotlin coroutines/DSLs are core to agent runtime | Build config may still use Groovy/Kotlin DSL; no Java classes in `app/` |
| 2025-01-15 | DL-002 | Jetpack Compose for UI | Need consistent, declarative UI for workspace, terminal, settings | Jetpack Compose, XML layouts, Compose + XML hybrid | Jetpack Compose | Declarative model fits reactive agent state; faster iteration; Material 3 first-class | No XML layout resources; all screens are `@Composable` functions |
| 2025-01-16 | DL-003 | Room for local database | Persist agents, tasks, conversation history, plugin configs | Room, SQLDelight, Realm, SQLite directly | Room | Official Jetpack lib; type-safe DAOs; Compose integration; migration tooling | Schema versioning strategy needed early; DAOs per domain module |
| 2025-01-16 | DL-004 | Hilt for dependency injection | Agent runtime, tool registry, and provider system all need DI | Hilt, Koin, manual DI, Kodein | Hilt | Compile-time safety; Android lifecycle scopes; minimal boilerplate vs manual | `@HiltAndroidApp` entry point; all modules expose `@Provides` |
| 2025-01-17 | DL-005 | OkHttp + Retrofit for networking | LLM provider APIs, plugin HTTP tools, webhook delivery | OkHttp+Retrofit, Ktor, Volley, Fuel | OkHttp + Retrofit | Mature ecosystem; interceptors for auth/logging; suspend function support | Shared `OkHttpClient` singleton; Retrofit interfaces per provider |
| 2025-01-17 | DL-006 | Kotlinx Serialization for JSON | Serialize agent state, tool configs, API payloads | Kotlinx Serialization, Gson, Moshi, Jackson | Kotlinx Serialization | Compile-time codegen; Kotlin-native; no reflection; works with sealed classes | `@Serializable` on all data classes; `Json` instance configured once |
| 2025-01-18 | DL-007 | DataStore for preferences | Store user settings, provider keys, workspace prefs | DataStore, SharedPreferences, encrypted SharedPreferences | DataStore Preferences | Coroutine-native; type-safe; no ANR risk from blocking I/O | Migration path from SharedPreferences not needed (greenfield) |
| 2025-01-18 | DL-008 | Material Design 3 theming | Consistent look across workspace, tasks, terminal, settings | Material 2, Material 3, custom design system | Material 3 (Material You) | Dynamic color support; Compose-native; modern aesthetic | `MaterialTheme` with custom `colorScheme`; dynamic color opt-in |
| 2025-01-19 | DL-009 | Coroutines + Flow for async | Agent execution, streaming LLM responses, UI state | Coroutines+Flow, RxJava, LiveData-only, async/await | Coroutines + Flow | First-party; lightweight; Compose integration via `collectAsState` | All async work uses `suspend`; state exposed as `StateFlow` |
| 2025-01-19 | DL-010 | WorkManager for background tasks | Scheduled agent runs, sync, cleanup | WorkManager, AlarmManager, Foreground Service, JobScheduler | WorkManager | Handles doze/battery optimization; constraints API; backoff built-in | Periodic tasks registered as `PeriodicWorkRequest`; one-off for sync |
| 2025-01-20 | DL-011 | Mermaid for diagram documentation | Architecture and flow diagrams in repo | Mermaid, PlantUML, ASCII art, external images | Mermaid | Renders in GitHub natively; text-based; easy to version | All ADRs and design docs use Mermaid code blocks |
| 2025-01-20 | DL-012 | Conventional Commits for git messages | Consistent changelog generation and history | Conventional Commits, freeform, Angular-style | Conventional Commits | Automates CHANGELOG; enables semantic versioning tooling | `commitlint` config; CI enforces format on PR merge |
| 2025-01-21 | DL-013 | API 34 as minimum SDK | Target modern Android; reduce compat surface | API 26, API 30, API 34 | API 34 (Android 14) | Access to modern APIs (predictive back, photo picker, granular perms) without compat libs | ~35% device coverage at launch; grows over time |
| 2025-01-21 | DL-014 | APK size target < 50MB | Keep install size manageable for on-device delivery | < 30MB, < 50MB, < 100MB, AAB-first | APK < 50MB | Balances feature richness with quick sideload; no Play Store size gate | ProGuard/R8 enabled; resource shrinking per build variant |
| 2025-01-22 | DL-015 | EventBus for inter-module communication | Decouple agent runtime, UI, and plugin system | EventBus, shared Flow, callback interfaces, BroadcastReceivers | EventBus pattern | Decouples modules without circular deps; typed events; easy to audit | Custom sealed `AppEvent` class; central `EventBus` singleton via Hilt |
| 2025-01-22 | DL-016 | NXR- prefix for all error codes | Unified error taxonomy across agent, tools, providers | NEX-, NXR-, APP-, un-prefixed | NXR- | Short, unique, searchable; avoids collision with system codes | All exceptions and error payloads use `NXR-XXXX` format |
| 2025-01-23 | DL-017 | Stable IDs in registries | Agent, tool, and provider registries need stable identifiers | UUID, auto-increment int, domain-prefixed (AGT-001, TOOL-001) | Domain-prefixed stable IDs | Human-readable; debuggable; no collision across registries | Format: `{ENTITY}-{NNN}`; enforced in schema and validation |
| 2025-01-23 | DL-018 | Bottom navigation with 4 tabs | Primary app navigation structure | Bottom nav 4 tabs, drawer nav, top tabs, rail nav | Bottom nav (4 tabs) | Standard Android pattern; thumb-reachable; maps to core user flows | Tabs: Workspace, Tasks, Terminal, Settings ⚠ **superseded by DL-019** (3-tab: Workspace, Tasks, Settings) |
| 2026-08-03 | DL-019 | Agent-first interaction model — infrastructure is internal | Sandbox, embedded terminal, runtimes, and execution engine were user-facing features (Terminal tab, Terminal screen, user-invocable sessions) | Keep infrastructure UI, hide infrastructure, hybrid | Hide infrastructure (ADR-0006) | Users interact with AI agents, not plumbing; agents auto-trigger tools/terminal in an isolated sandbox; simpler UX and smaller UI surface on mobile | Amends DL-018 (3-tab bottom nav: Workspace, Tasks, Settings); terminal output surfaces in the chat activity feed; optional developer mode later |
| 2026-08-06 | DL-020 | MCP Integration (Category 27) — first-class tool source alongside built-in and plugin | Industry precedent verified (`bitdoze.com` 2026-07-24; `mcp.directory` 2026-07-09; `aihackers.net` 2026-07-03; `blog.4sapi.com` 2026-07-07). MCP servers provide external capabilities through stdio + Streamable HTTP transport, capability negotiation, and tool/resource/prompt primitives | Own protocol contracts (`Provider-Protocol.md`, `Tool-Protocol.md`), native MCP standard (stdio/HTTP/SSE), hybrid | Own protocol contracts (same extensibility goal, different mechanism — `protocols/Provider-Protocol.md` + `PluginSDK`) — aligns with existing `AIProvider` abstraction and `CapabilityRegistrar` without redesign | MCP primitives (`tool`/`resource`/`prompt`) map onto existing `Tool` interface; transport isolation applies (`sandbox:execute`/`network:http`); deny-by-default (`G2`) applies to undeclared server scopes; no new architecture module; registry entries (`TOOL-397`..`TOOL-402`) and matrix updated | Category 27 added to `architecture/TOOL_SYSTEM.md`; registry entries (`TOOL-397`..`TOOL-402`) added; `protocols/Tool-Protocol.md` updated (§MCP Tool Mapping); audit confirms difference (`Implemented Differently`) |
| 2026-08-06 | DL-021 | OEM battery-optimization onboarding + graceful degradation to WorkManager-only | Verified lesson from Kimi Claw / MiniMax Hailuo (`aihackers.net` 2026-07-03; `digitalapplied.com` 2026-07-03): OEM battery managers kill background processes regardless of Android API rules | Ignore OEM rules, enforce via foreground service only, user-guided settings flow + graceful degradation ladder | User-guided settings flow (`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`) + graceful degradation ladder (`FR-AS-009` degradation ladder extended) — if user denies battery optimization, agent degrades to `WorkManager`-only (`reduced autonomy`: no long-running foreground service, shorter checkpoint interval, no real-time progress notification) | Adds detection and settings surface to `specs/BACKGROUND_EXECUTION.md`; degradation rules (`WorkManager`-only mode with reduced autonomy) documented; references `FR-T011` (scheduled jobs — `WorkManager`-backed) and `FR-AS-009` (fault-injection + degradation ladder); `security/PermissionModel.md` unchanged (`device:notifications` remains `ASK`) | Phase 2 (`specs/BACKGROUND_EXECUTION.md`); documentation-level change only — no new architecture |
| 2026-08-06 | DL-022 | Deny-by-default security posture + optional on-device auto-approval classifier (TFLite) *(superseded: scope defaults revised by DL-034; classifier wording revised S13)* | Verified `~93%` approval-fatigue finding from Claude research (`bitdoze.com` 2026-07-24; `blog.4sapi.com` 2026-07-07) | Depend solely on user approval (`FR-S016` manual/autopilot + permission scopes), add TFLite classifier as primary layer (replacing user approval), optional TFLite as independent secondary layer | Optional TFLite classifier (`security/PermissionModel.md` — new `AutoApprovalClassifier` section) as independent secondary layer: operates after permission check but before execution; can `DENY` regardless of `ALLOW`; user can disable in settings; does not replace user approval (`FR-S016` autonomy modes preserved); default `DENY` principle enforced for riskiest scopes (`sandbox:execute` + `plugin:install` + `device:*` — already `DENY`/`ASK` defaults, strengthened with explicit principle) | `security/PermissionModel.md` updated (§Deny-By-Default + §Auto-Approval Classifier); `specs/CONTEXT_MANAGEMENT.md` referenced (`FR-EV-003` zero-assumption mode); `FR-EV-002` structured confidence (`LOW` triggers `ASK` — supports independent classifier behavior); `FR-AS-005` (trust growth) unchanged | Security hardening (G2) — documentation/spec level; no new architecture module (`PermissionModel.md` owns the change) |
| 2026-08-06 | DL-023 | Sensitive-app / high-risk-domain blocked-list for browser automation | Verified Kimi Claw pattern (`aihackers.net` 2026-07-03; `digitalapplied.com` 2026-07-03): sensitive accounts (banking/payment/trading/insurance) must not be automated due to regulatory and fraud risk | No blocked-list (agent relies solely on permission scopes + sandbox rules), full blocked-list with automatic isolation (new architecture module) | Blocked-list (`security/SandboxPolicy.md` — new `BlockedDomainsAndApps` table) + isolation warning (`specs/BROWSER.md` — new `BlockedListWarning` section): if agent attempts browser automation on blocked domain/app class, sandbox denies (`NXR-7005`) with audit log (`FR-T015`); user must isolate sensitive accounts (separate workspace + separate profile — `FR-W001` workspace isolation + `FR-P011` provider profile isolation) before any browser automation; no redesign — uses existing sandbox/network rules | `security/SandboxPolicy.md` updated (§Blocked Domains and Sensitive Apps); `specs/BROWSER.md` updated (§Blocked List + Isolation Warning); `FR.md` references (`FR-S014` network egress policy, `FR-W005` workspace settings); audit log preserved (`FR-T015` — execution logging + audit trail) | Security hardening (G3) — documentation/spec level; no redesign; existing sandbox/network rules extended |
| 2026-08-06 | DL-024 | Real-time voice input + live camera as first-class agent I/O (roadmap/optional) | Verified Grok / MiniMax Hailuo patterns (`aihackers.net` 2026-07-03; `flowtivity.ai` 2026-07-12): voice and camera as native input modes (not just chat attachments). Gated by existing permission scopes (`device:camera` `DENY`, `device:storage` `DENY` — `security/PermissionModel.md`) | Add new input architecture (new interface/module for voice/camera streaming), extend existing `Tool` interface with voice/camera categories (existing `Android Device` category — `device_camera` `TOOL-??`, `device_audio` `TOOL-??`) | Extend existing `Android Device` category (`architecture/TOOL_SYSTEM.md` §Category 18 — `device_camera`, `device_audio`) to specify real-time streaming behavior (not just snapshot/file access): `device_camera` (`TOOL-???`) supports continuous stream (`Streaming` flag `✓`) for live agent observation; `device_audio` (`TOOL-???`) supports real-time transcription stream (`Streaming` flag `✓` — `ai_transcribe` `TOOL-101` already supports streaming). Default `DENY` (`security/PermissionModel.md`) preserved; user must explicitly grant (`FR-S016` autonomy mode must be `Manual` or `Assisted` for `device:*` scopes). Marked `Later` / optional (`Phase 5` or later) — does not require new architecture module (`Android Device` category exists; streaming support exists via `Streaming` capability in `registry/TOOL_MATRIX.md`) | `architecture/TOOL_SYSTEM.md` updated (§Android Device — real-time streaming note); `specs/AI_PROVIDERS.md` referenced (vision capabilities — `ai_image_analyze` `TOOL-103` supports vision; streaming voice aligns with `ai_transcribe`/`ai_speech`); `security/PermissionModel.md` unchanged (`device:camera`/`device:audio` remain `DENY` default); `FR.md` references (`FR-S001`..`FR-S028` — sandbox isolation for device access; `FR-S016` — autonomy modes control approval gates); `docs/ROADMAP.md` (Phase 5 or later — optional) | Feature roadmap (G5) — documentation/spec level; no new architecture; existing categories and scopes preserved; optional/roadmap status clearly marked |

| `DL-027` | `S3 Lifecycle fill (Option A)` | `VERIFIED` (`lifecycle/*.md` expanded; `ARCHITECTURE.md` references preserved) | `FR-EL-007`, `FR-AS-013`, `FR-M001`, `FR-TE001` | `docs/LIFECYCLES.md` + `CHANGELOG.md` + `DECISION_LOG.md` | `Active` (Phase 7) | `lifecycle/*.md` (4 files filled) + `ARCHITECTURE.md` (valid) + `CANONICAL_SOURCES.md` (unchanged) + `MODULE_BOUNDARIES.md` (updated) |
| `DL-028` | `S4 Terminal spec fully specified` | `VERIFIED` (`TERMINAL.md` expanded; `TerminalSession.md` fields added; sync files updated) | `FR-TE001`..`005`, `FR-S002`/`003`/`018`, `FR-AS-002`/`003`/`009`/`013`, `FR-T015`, `FR-EV-002`/`006`, `FR-M012`/`013`, `FR-A010`, `FR-U005` | `docs/CHANGELOG.md` + `DECISION_LOG.md` + `FR_NFR_MAPPING.md` + sync docs | `Active` (Phase 4) | `specs/TERMINAL.md` (expanded) + `models/TerminalSession.md` (new fields) + sync docs (`ARCHITECTURE.md`, `SandboxPolicy.md`, `BACKGROUND_EXECUTION.md`, `Execution-Protocol.md`) |
| `DL-029` | `S3-E State-machine companions for Workspace, Memory, TerminalSession` | `VERIFIED` (3 new `state-machines/*.md` created; lifecycle stubs linked; CAS sources updated) | `FR-EL-007`, `FR-AS-013`, `FR-M001`, `FR-TE001` | `state-machines/` (3 new) + `lifecycle/*.md` (4 updated) + `docs/CANONICAL_SOURCES.md` + `ARCHITECTURE.md` + `MODULE_BOUNDARIES.md` + `models/` (3 updated) + `CHANGELOG.md` | `Active` (Phase 7) | `state-machines/WorkspaceLifecycle.md` (Canonical: Created/Active/Suspended/Archived/Deleted), `state-machines/MemoryLifecycle.md` (Canonical: Recorded/Indexed/Retrieved/Retained/Expired/Deleted), `state-machines/TerminalSessionLifecycle.md` (Canonical: Created/Attached/Running/Detached/Closed/Failed); lifecycle stubs now DERIVED + link to state machines; models link to state machines |
| 2026-08-06 | DL-030 | Multi-Instance Pipes — zero-config instance discovery, pairing, and cross-instance delegation | Nexora had no instance-to-instance collaboration (gap vs peer products: main/sub instances, LAN discovery, broadcast routing); existing multi-agent model (SA-1..SA-5) is intra-workspace only | Build pipes as transport extension of SA-1..SA-5; build a separate remote-execution subsystem; defer entirely | Pipes as transport extension (coordinator role and FR-AG-002 preserved; remote sub-agent is a first-class Task running in the remote instance sandbox with the remote instance provider profiles) | Reuses proven contracts (SA-1..SA-5, TaskLifecycle, PermissionModel, DLP egress, quarantine) instead of inventing a parallel remote-execution stack; deny-by-default pairing + broadcast keeps the trust surface minimal; ADR-0006 respected (Settings → Pipes surface, activity-feed badges, no new chat tab or slash commands) | New canonical spec `specs/PIPES.md`; new canonical state machine `state-machines/InstanceLifecycle.md`; new model `models/Instance.md`; `FR-MI-001`..`010`; `NFR-SEC-014`; `TOOL-405`..`408`; `FEAT-034`; `instance:*` scopes in `security/PermissionModel.md`; `CANONICAL_SOURCES.md` + `TRACEABILITY.md` + `REQUIREMENT_COVERAGE_LEDGER.md` + `FR_NFR_MAPPING.md` updated (S5) |
| 2026-08-06 | DL-031 | Reasoning effort control — 6-level scale (OFF/LOW/MEDIUM/HIGH/X_HIGH/MAX) with per-scope override | Peer products expose Admin UI → Model Config → Reasoning with disable + 5 levels; Nexora had only 3 classification levels (fast/balanced/thorough) with no disable and no settings surface | Extend scale + add OFF + settings surface; keep 3 levels; per-task only override | 6-level scale with OFF (reasoning disabled: gate bypassed, reasoning params omitted from requests, REASONING models never selected) + override hierarchy task→agent→workspace→global→default(MEDIUM) + Settings → Model Config → Reasoning surface | Matches peer capability granularity without a chat-embedded toggle (ADR-0006); OFF is absolute per scope but RG/EV evidence gates stay active (deliberation depth is removed, never evidence discipline); override hierarchy mirrors PermissionModel layering (consistency) | `specs/CONTEXT_MANAGEMENT.md` §6 (Reasoning Effort Scale); `requirements/FR.md` FR-RN-003/004 amended + FR-RN-007/008 added; `architecture/PROVIDER_SYSTEM.md` `ReasoningEffort` enum + `CompletionRequest.reasoningEffort` + mapping rules; `docs/TRACEABILITY.md` FR-RN-007 row; `docs/FR_NFR_MAPPING.md` S6 note; `docs/CHANGELOG.md` entry |
| 2026-08-06 | DL-033 | Multi-scope permission evaluation — checkPermission now evaluates every required scope with aggregated ASK approval; unknown scopes immediately denied via PermissionScopeRegistry | S12 audit correction (NX-HIGH-001). Previous pseudocode returned on first scope only — multi-scope tools like TOOL-408 (instance:delegate + agent:create) only evaluated one scope. | Sequential approval (one ASK at a time), aggregated approval (all ASK scopes in one transaction), or fix only the premature-return bug | Aggregated approval — all ASK scopes presented in one transaction to reduce approval fatigue; any scope DENY returns immediately; ALLOW continues to remaining scopes; order-independent | Preserves the existing Agent → Workspace → Global → scope default hierarchy; reduces user interruption for multi-scope tools; no workflow change for single-scope tools. | security/PermissionModel.md (checkPermission pseudocode), architecture/SECURITY_MODEL.md (summarized flow), TOOL-408 (pipe_delegate regression target) |
| 2026-08-06 | DL-034 | Explicit risk-based scope defaults replace contradictory deny-by-default principle (G2) | S12 audit correction (NX-HIGH-002). Previous G2 section (§Deny-By-Default Principle) claimed plugin:install and agent:create were default DENY (line 160) while the scope table showed ASK (lines 38-39); impact table simultaneously claimed defaults changed (lines 172,174) and remained unchanged (line 178). | Retain DENY defaults for high-risk scopes (change scope table), retain ASK defaults (keep scope table and remove contradicting prose), or preserve scope table and rewrite the principle to clarify without contradiction | Preserve scope table as authoritative (plugin:install=ASK, agent:create=ASK); rewrite deny-by-default as explicit risk-based defaults with clear ALLOW/ASK/DENY semantics; no prose overlay reinterprets table values | Least-disruptive resolution — preserves existing product behavior (ASK requires user approval); scope table is the single source of truth; no requirement changes needed. | security/PermissionModel.md (§Explicit Risk-Based Scope Defaults) |
| 2026-08-06 | DL-035 | Canonical Session lifecycle — new state machine (SessionLifecycle.md) with CREATED/ACTIVE/IDLE/CLOSED/EXPIRED | S12 audit correction (NX-HIGH-004, NX-GAP-001). Previous lifecycle/SessionLifecycle.md contained two incompatible state sets: CREATED/ACTIVE/IDLE/CLOSED/EXPIRED (§States) and Initiated/Paused/Completed/Failed/Restored (§Expanded S3). Model enum (SessionStatus) used the first set with no aliasing to the second. No canonical ownership declared in CANONICAL_SOURCES. | Keep both state sets (declare aliases), pick one set, or create a new set | Canonical state machine with CREATED/ACTIVE/IDLE/CLOSED/EXPIRED — matches model enum; Session lifecycle separate from Task/Execution lifecycle; removed incompatible Initiated/Paused/Completed/Failed/Restored states | Model enum already uses these states; IDLE captures context retention (≠ Paused which is Task-level); CLOSED/EXPIRED are terminal (≠ Completed/Failed which are Task-level); no breaking change to existing model. | state-machines/SessionLifecycle.md (new canonical), lifecycle/SessionLifecycle.md (rewritten as SUPPORTING), models/Session.md (updated lifecycle pointer), docs/CANONICAL_SOURCES.md (+row) |
| 2026-08-06 | DL-036 | ExecutionStatus lifecycle ownership — RUNTIME.md §ExecutionStatus Lifecycle declares Executor as canonical owner with authoritative transitions | S12 audit correction (NX-GAP-002). ExecutionStatus (CREATED/RUNNING/COMPLETED/FAILED/CANCELLED) was defined in models/Execution.md but had no declared lifecycle owner. | Create state-machines/ExecutionLifecycle.md, declare RUNTIME.md Executor as owner, or leave ownership implicit | Declare RUNTIME.md §ExecutionStatus Lifecycle as canonical — 5 transitions with guards; 8 rules; checkpoint resume retains same executionId with version increment; new identity only for explicit retry after terminal status. | Clears the S12 GAP-002 finding. Execution lifecycle is now deterministic: same-identity resume for checkpoints, new-identity for terminal retry. BACKGROUND_EXECUTION.md and AUTONOMY_STABILITY.md recovery policies are consistent with this rule. | architecture/RUNTIME.md (§ExecutionStatus Lifecycle), specs/BACKGROUND_EXECUTION.md, specs/AUTONOMY_STABILITY.md, docs/CANONICAL_SOURCES.md (+row) |
| 2026-08-06 | DL-037 | ToolStatus lifecycle — TOOL_SYSTEM.md §ToolStatus Lifecycle defines canonical transitions (DISCOVERED→REGISTERED→ACTIVE↔DISABLED) | S12 audit correction. ToolStatus (DISCOVERED/REGISTERED/ACTIVE/DISABLED) was defined in models/Tool.md but had no transition guards, authority, or canonical lifecycle document. Tool descriptor lifecycle is distinct from per-call ToolExecution — a failed invocation does not change ToolStatus to DISABLED. | Create state-machines/ToolLifecycle.md, add transition section to TOOL_SYSTEM.md, or leave implicit | Add §ToolStatus Lifecycle to TOOL_SYSTEM.md — 4 transitions with explicit guards; distinction from ToolExecution documented; guard rules defined (validation, plugin health, admin action). No new file — TOOL_SYSTEM already owns tool subsystem. | Clears the S12 LOW-001 finding. Tool descriptor availability is now governed by explicit transition rules rather than implicit tool-registry behavior. Tool call execution remains separate. | architecture/TOOL_SYSTEM.md (§ToolStatus Lifecycle), docs/CANONICAL_SOURCES.md (+row) |
